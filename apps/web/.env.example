NODE_ENV=development
DATABASE_URL=postgresql://berean_study:berean_study@localhost:5436/berean_study
BETTER_AUTH_SECRET=replace-with-a-random-secret-of-at-least-32-characters
BETTER_AUTH_URL=http://localhost:3001
RESEND_FROM_EMAIL=no-reply@example.com

# Choose exactly one email provider configuration.
EMAIL_PROVIDER=smtp
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASS=

# EMAIL_PROVIDER=resend
# RESEND_API_KEY=replace-with-your-resend-api-key

# Development and test only. Validation is never skipped in production.
# SKIP_ENV_VALIDATION=true
